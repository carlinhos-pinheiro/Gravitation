const planets  = [];
const holes = [];
const vectors = [];

class Constants{
  static G = Math.pow(6.6, -11);
  static scale = 0.125*Math.pow(10,10); // One pixel is equal to 500000m
  static seconds = 150 //this will a constant representing the time inside de simulation each frame.
}